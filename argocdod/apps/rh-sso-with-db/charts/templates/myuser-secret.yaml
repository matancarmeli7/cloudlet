apiVersion: v1
stringData:
  password: "{{.Values.global.pg_password}}"
  username: "{{.Values.global.pg_username}}"
kind: Secret
metadata:
  labels:
    pg-cluster: "{{.Values.global.pg_cluster_name }}"
  name: "{{.Values.global.pg_cluster_name}}-myuser-secret"
  namespace: "{{.Values.global.pg_namespace}}"
type: Opaque
