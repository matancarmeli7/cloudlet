apiVersion: v1
kind: Service
metadata:
  name: {{ include "crunchy-operator.fullname" . }}
  labels:
    {{- include "crunchy-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crunchy-operator.selectorLabels" . | nindent 4 }}
